/** Dataset and ingest — OWNED BY U1. Placeholder from the integrator shell; replace wholesale. */
export default function DataSource() {
  return (
    <section className="rounded-card border border-dashed border-ink-300 p-4 text-sm text-ink-500">
      <p className="font-medium text-ink-700">U1 — Dataset and ingest</p>
      <p className="mt-1">Not built yet. This panel appears here once U1 merges.</p>
    </section>
  )
}
