/** Per-student trace — OWNED BY U3. Placeholder from the integrator shell; replace wholesale. */
export default function StudentTrace() {
  return (
    <section className="rounded-card border border-dashed border-ink-300 p-4 text-sm text-ink-500">
      <p className="font-medium text-ink-700">U3 — Per-student trace</p>
      <p className="mt-1">Not built yet. This panel appears here once U3 merges.</p>
    </section>
  )
}
